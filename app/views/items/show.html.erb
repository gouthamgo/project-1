<%= link_to "Back", items_path %>
<h1><%= @item.name %></h1>
  <div class="grid">
    <div class="g-col-6">
      <%= image_tag @item.image, :class =>  'feature' if @item.image.present? %>
    </div>
      <br>
      <div class="description">
        <% if @item.user.present? %>


        <% end %>
          <h5>Description:</h5>
            <%= @item.description %>

              <h3><%= number_to_currency @item.price %></h3>

              <ul>
                <li>Name: <%= @item.name %></li>
                <li>Price: $<%= @item.price %>/per 2 days</li>

              </ul>


                <div class="btn-group">
                    <%= button_to "Add  to cart", line_items_path(:item_id => @item.id), :method => 'post', :class=>"btn btn-success" %>
                    </div>



        </div>
  </div>
